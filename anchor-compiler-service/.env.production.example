# ===== HETZNER PRODUCTION CONFIG =====

# Server
NODE_ENV=production
PORT=5000

# Docker
DOCKER_IMAGE=anchor-builder:latest
BUILD_TIMEOUT=600

# Domain
DOMAIN=api.opencompiler.io
CORS_ORIGIN=https://opencompiler.io

# API Key (generate with: openssl rand -hex 32)
API_KEY=

# Agent admin key (generate with: openssl rand -hex 32)
AGENT_ADMIN_KEY=

# Anthropic AI (required for smart build / AI features)
ANTHROPIC_API_KEY=

# Logging
LOG_LEVEL=info

# ===== DEPLOYMENT CHECKLIST =====
#
# 1. Copy this file: cp .env.production.example .env.production
# 2. Generate API_KEY: openssl rand -hex 32
# 3. Generate AGENT_ADMIN_KEY: openssl rand -hex 32
# 4. Add ANTHROPIC_API_KEY from https://console.anthropic.com/
# 5. Build Docker image: docker build -f Dockerfile.anchor-builder -t anchor-builder:latest .
# 6. Start: docker compose -f docker-compose.production.yml up -d
# 7. Verify: curl https://api.opencompiler.io/health
